spring:
  application:
    name: user-management-service
  cloud:
    config:
      uri: http://localhost:8888
      username: user
      password: 1234
      fail-fast: true
  main:
    allow-bean-definition-overriding: true

